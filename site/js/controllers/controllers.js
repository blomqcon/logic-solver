var logicControllers = angular.module('logicControllers', []);

logicControllers.controller("HomeCtrl", ["$scope", "$location", "$window",function ($scope, $location, $window) {
	
}]);

logicControllers.controller("ProofSolveCtrl", ["$scope", "$location", "$window",function ($scope, $location, $window) {
	
}]);